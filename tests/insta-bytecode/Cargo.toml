[package]
name = "insta-bytecode"
publish = false
version.workspace = true
edition.workspace = true

[dependencies]
boa_engine = { workspace = true, features = ["trace"] }

[dev-dependencies]
insta = { version = "1.47.2", features = ["filters", "glob"] }
