[package]
name = "boa_wpt"
publish = false
version = "0.1.0"
edition = "2021"

[dependencies]
boa_engine.workspace = true
boa_gc.workspace = true
boa_interop.workspace = true
boa_runtime.workspace = true
rstest.workspace = true

[build-dependencies]
git2 = "0.20.2"
toml = "0.8.23"
serde = { workspace = true, features = ["derive"] }

[features]
# Run the suites of tests. Without this flag (which is not enabled by
# default), the tests will be ignored as they require an environment
# variable that isn't set when running tests normally.
wpt = []
